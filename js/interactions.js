(()=>{const line=document.getElementById('messageMemoryLine');const buttons=[...document.querySelectorAll('.memory-button')];if(!line||!buttons.length)return;const render=value=>{line.replaceChildren();value.split('|').forEach((part,index)=>{if(index)line.appendChild(document.createElement('br'));line.appendChild(document.createTextNode(part))})};buttons.forEach(button=>button.addEventListener('click',()=>{if(button.classList.contains('is-active'))return;buttons.forEach(item=>{const active=item===button;item.classList.toggle('is-active',active);item.setAttribute('aria-pressed',String(active))});line.classList.add('is-changing');setTimeout(()=>{render(button.dataset.message);line.classList.remove('is-changing')},220)}))})();
