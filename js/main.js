const gate=document.getElementById('gate');const enter=document.getElementById('enterBtn');const audio=document.getElementById('bgm');const sound=document.getElementById('soundBtn');const progress=document.getElementById('progressBar');const last=document.getElementById('lastBtn');const finale=document.getElementById('finale');
enter.addEventListener('click',async()=>{gate.classList.add('open');document.body.classList.remove('locked');sound.classList.add('visible');try{audio.volume=.42;await audio.play()}catch(e){sound.classList.add('muted');sound.setAttribute('aria-pressed','false')}setTimeout(()=>document.querySelector('.opening .reveal')?.classList.add('is-visible'),450)});
sound.addEventListener('click',async()=>{if(audio.paused){try{await audio.play();sound.classList.remove('muted');sound.setAttribute('aria-label','暂停背景音乐');sound.setAttribute('aria-pressed','true')}catch(e){}}else{audio.pause();sound.classList.add('muted');sound.setAttribute('aria-label','播放背景音乐');sound.setAttribute('aria-pressed','false')}});
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}}),{threshold:.16,rootMargin:'0px 0px -6%'});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
let ticking=false;addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(()=>{const max=document.documentElement.scrollHeight-innerHeight;progress.style.height=(max>0?scrollY/max*100:0)+'%';ticking=false});ticking=true}},{passive:true});
document.querySelectorAll('.holdable').forEach(el=>{let timer;const start=()=>timer=setTimeout(()=>el.classList.add('held'),450);const stop=()=>{clearTimeout(timer);setTimeout(()=>el.classList.remove('held'),900)};el.addEventListener('touchstart',start,{passive:true});el.addEventListener('touchend',stop);el.addEventListener('mousedown',start);el.addEventListener('mouseup',stop);el.addEventListener('mouseleave',stop)});
last.addEventListener('click',()=>{last.disabled=true;last.style.opacity='0';setTimeout(()=>{finale.classList.add('show');finale.setAttribute('aria-hidden','false');finale.scrollIntoView({behavior:'smooth'});setTimeout(()=>finale.querySelector('.finale-inner').animate([{opacity:0,transform:'translateY(24px)'},{opacity:1,transform:'none'}],{duration:1600,fill:'both',easing:'ease'}),500)},500)});
